# Cloud Functions (server)

This folder should contain Firebase Cloud Functions for Mollie webhooks and server-side logic, e.g.:

- `mollieWebhook` — handle payment webhooks and idempotently credit `walletTransactions`.

Implement functions in a dedicated Firebase Functions project and deploy separately.
