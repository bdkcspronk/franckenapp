Francken Mobile App
===================

Minimal scaffold for the Francken mobile app (Expo + Firebase).

Setup
-----

1. Install dependencies:

   npm install

2. Start Expo:

   npx expo start

Replace the placeholder Firebase config in `src/firebase/config.js` before using auth or DB.
